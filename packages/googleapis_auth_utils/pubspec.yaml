name: googleapis_auth_utils
description: Utilities for working with googleapis_auth package.
version: 0.1.0
repository: "https://github.com/invertase/dart_firebase_admin"

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  googleapis_auth: ^1.3.0
  http: ^1.0.0

dev_dependencies:
  test: ^1.24.4
